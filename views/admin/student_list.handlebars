<div class="container">
	<div class="panel panel-default">
    <a href="/admin/student/create" class="btn">Add Student</a>
    <div class="panel-heading">
      <h3>Students</h3>
    </div>

    <div class="panel-body">
      <table class="table">
        <thead>
          <tr>
            <th>Student Number</th>
            <th>Name</th>
            <th>Email</th>
            <th>Phone Number</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
          {{#each studentList}}
            <tr>
              <td>{{this.student_id}}</td>
              <td>{{this.prefix}} {{this.first_name}} {{this.middle_name}} {{this.last_name}} {{this.suffix}}</td>
              <td>{{this.email}}</td>
              <td>{{this.phone_number}}</td>
              <td><a href="#">view</a></td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    </div>

  </div>
</div>